{
  "name": "whatnot-purchase-bot",
  "version": "0.1.3",
  "private": true,
  "description": "Permissioned Chrome extension demonstration for direct Whatnot UI bidding.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "validate": "node scripts/validate.mjs"
  }
}
